const assert = require('chai').assert;
const expect = require('chai').expect;
const app = require('../fizzBuzz.js');

describe.only('fizzBuzz', function() {
    it('should exist', function() {
        expect(fizzBuzz).to.exist; 
    })
})